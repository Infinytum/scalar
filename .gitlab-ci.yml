image: registry.infinytum.co/container-images/builder

stages:
  - release

before_script:
  - custom-login

base:
  stage: release
  only:
    - master
  script:
    - cat /kaniko/.docker/config.json
    - kbuild infinytum/scalar